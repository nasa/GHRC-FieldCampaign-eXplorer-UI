import {campaign, logo, description, dois, links, legends, defaultCamera} from "./constantLayers";
import instrumentLayers from "./instrumentLayers";

export {campaign, logo, description, dois, links, legends, defaultCamera, instrumentLayers}